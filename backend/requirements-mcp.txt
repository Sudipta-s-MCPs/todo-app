# MCP Server requirements - Ultra minimal
# Only what's needed for MCP functionality

# MCP Server framework
fastmcp==2.9.2

# HTTP client for API calls
httpx>=0.28.1

# Data validation
pydantic>=2.5.0,<2.10.0

# Environment variables
python-dotenv==1.1.0

# JSON handling is built into Python
# UUID handling is built into Python

# Note: This is MUCH smaller than full backend requirements
# No database drivers, no FastAPI, no authentication, no AI/ML
# Total size should be under 100MB