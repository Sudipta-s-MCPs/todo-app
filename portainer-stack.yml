version: '3.9'

services:
  backend:
    image: ghcr.io/sudipta-s-mcps/smart-todo-backend:latest
    ports:
      - "5482:8000"
    environment:
      # Core Infrastructure
      - ENVIRONMENT=production
      - DATABASE_URL=postgresql+asyncpg://sd_todo_app_user:AAqLX5r0lzm53hgQu48XIClw@192.168.11.100:15432/sd_todo_app_db
      - REDIS_URL=redis://192.168.11.100:6379/0
      - SECRET_KEY=qU0kgMpWIkaZkrX2SYbxmRjfv8KJEho0V9u5u6kXAiTx2ecEo7KgXZtwx
      - ALGORITHM=HS256
      
      # API Configuration
      - API_V1_PREFIX=/api/v1
      - API_BASE_URL=https://todo-api.sudiptadhara.in
      - CORS_ORIGINS=["http://192.168.11.108:5483", "http://192.168.11.108:5484", "http://192.168.11.108:5482", "https://192.168.11.108:5483", "https://192.168.11.108:5484", "https://192.168.11.108:5482", "https://todo-api.sudiptadhara.in"]
      
      # MCP Configuration
      - MCP_SERVER_NAME=TodoApp
      - MCP_SERVER_VERSION=1.0.0
      
      # Admin Configuration
      - ADMIN_USERS=["sudiptai26.889@gmail.com"]
      
      # Test Configuration
      - TEST_API_KEY=v99KUrYXhQAMTRg41JTG203cPGi30ajCWhRmfmLzfC4F2wJsujDH
      
      # Monitoring
      - LOG_LEVEL=INFO
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
        delay: 10s
        max_attempts: 3
      resources:
        limits:
          memory: 512M
        reservations:
          memory: 256M
    # healthcheck:
    #   test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
    #   interval: 30s
    #   timeout: 10s
    #   retries: 3
    #   start_period: 40s

  frontend:
    image: ghcr.io/sudipta-s-mcps/smart-todo-frontend:latest
    ports:
      - "5484:80"
    environment:
      - VITE_API_URL=https://todo-api.sudiptadhara.in/api/v1
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 3
      resources:
        limits:
          memory: 128M
        reservations:
          memory: 64M
    # healthcheck:
    #   test: ["CMD", "curl", "-f", "http://localhost:80/"]
    #   interval: 30s
    #   timeout: 10s
    #   retries: 3
    #   start_period: 20s

  admin-panel:
    image: ghcr.io/sudipta-s-mcps/smart-todo-admin:latest
    ports:
      - "5483:3000"
    environment:
      - VITE_API_URL=https://todo-api.sudiptadhara.in/api/v1
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 3
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 128M
    # healthcheck:
    #   test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost/"]
    #   interval: 30s
    #   timeout: 10s
    #   retries: 3
    #   start_period: 30s

  mcp-server:
    image: ghcr.io/sudipta-s-mcps/smart-todo-mcp:latest
    ports:
      - "5485:5485"
    environment:
      - TODO_API_ENDPOINT=https://todo-api.sudiptadhara.in/api/v1
      - MCP_SERVER_NAME=TodoApp
      - MCP_SERVER_VERSION=1.0.0
      - PYTHONPATH=/app
      # No authentication here - each MCP client authenticates independently
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
        delay: 10s
        max_attempts: 3
      resources:
        limits:
          memory: 512M
        reservations:
          memory: 256M
    # healthcheck:
    #   test: ["CMD", "curl", "-f", "http://localhost:5485/mcp/"]
    #   interval: 30s
    #   timeout: 10s
    #   retries: 3
    #   start_period: 30s
