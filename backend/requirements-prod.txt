# Production requirements - Minimal dependencies only
# Core FastAPI framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic[email]>=2.5.0,<2.10.0
pydantic-settings>=2.1.0,<2.10.0

# Database
sqlalchemy==2.0.23
asyncpg==0.29.0
alembic==1.13.0

# Redis
redis==5.0.1

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart>=0.0.9

# HTTP client
httpx>=0.28.1

# WebSocket support
websockets==12.0

# CORS and security
python-dotenv==1.1.0

# System monitoring (lightweight)
psutil==5.9.8

# LDAP Authentication
ldap3==2.9.1

# Object Storage (if needed)
minio==7.2.0

# Note: Removed heavy dependencies:
# - sentence-transformers (800MB+ with PyTorch)
# - qdrant-client, huggingface-hub, groq (AI/ML stack)
# - pytest, black, flake8, mypy (development tools)
# - celery (if not actively used)
# - pillow, qrcode (image processing)
# - sentry-sdk (monitoring - add back if needed)